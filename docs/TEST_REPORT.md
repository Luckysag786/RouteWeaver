# 测试报告

测试日期：2026-08-05（Asia/Shanghai）

## 自动化测试

- Python：34 项通过。覆盖规则模式、应用/域名匹配、规则修改规范化与查重基础、连接活动快捷规则、禁用规则、IDN、配置迁移、启动/托盘默认值、Windows 应用路径解析、HTTP/SOCKS5 上游解析、本地网关正反向转发、应用/网站规则级真实出口探针，以及 Windows 单实例进程锁。
- Android：4 项通过。覆盖正向包名/域名、反向包名、通配符、Unicode 域名与提供商交接规则摘要；Gradle `testDebugUnitTest` 与 `assembleDebug` 共 37 个任务成功。

## 第三方 VPN 实网测试

测试上游：本机第三方 VPN 提供的 HTTP 与 SOCKS5 入口。测试脚本未关闭、重启或写入 VPN 客户端。

| 模式 | 规则 | 命中出口 | 未命中出口 | 结果 |
|---|---|---|---|---|
| 正向 | `ifconfig.co` 网站 | VPN 海外出口 | 国内网络直连 | 通过 |
| 反向 | `ifconfig.co` 网站 | 国内网络直连 | VPN 海外出口 | 通过 |
| 正向 | `curl.exe` 应用 | VPN 海外出口 | `python.exe` 国内直连 | 通过 |
| 反向 | `curl.exe` 应用 | 国内网络直连 | `python.exe` VPN 出口 | 通过 |
| 正向 SOCKS5 | `ifconfig.co` 网站 | VPN 海外出口 | 国内网络直连 | 通过 |

规则级出口探针复测：正向 `curl.exe` 判定 VPN 并返回海外出口；反向 `*.example.com` 判定直连并返回国内出口。公开报告已脱敏，应用规则同时要求以“连接活动”中的实际进程命中记录作最终交叉验证。

完整机器可读记录：[artifacts/live-vpn-test.json](../artifacts/live-vpn-test.json)。

本轮连续执行 3 轮新连接复测，每轮 5 项全部通过；每轮前后系统代理均精确恢复到测试前状态。公开报告不记录用户的实际本地代理端口。

## 系统代理事务测试

第三方代理入口 → RouteWeaver 本地网关 → 原第三方代理入口，原值精确恢复。VPN 客户端相关进程的 PID 测试前后均未改变。

## Windows 安装冒烟

- 静默安装：退出码 0。
- 安装后的 EXE：启动 4 秒后仍稳定运行。
- 程序选择器实读：运行进程 292 项；Windows 桌面/MSIX 应用 191 项。
- 点击主窗口 ×：窗口隐藏，托盘后台进程保持运行；没有执行分流停用或代理恢复。
- 安装器默认勾选开机启动；取消勾选时不创建启动项。卸载会删除由安装器或应用创建的启动项。
- 静默卸载：退出码 0。
- 测试前后系统代理值完全一致。
- 1.3.1 打包程序复测：可见窗口正常启动；点击 × 后窗口数由 1 变为 0，两个打包进程继续驻留托盘；规则修改/删除 UI 冒烟通过。系统中已有 1.3.0 安装记录，因此本轮未覆盖或卸载用户现有安装，安装器仅完成编译与文件校验。
- 1.3.2 实网与打包复测：直连和 VPN 公网出口均可获取且地址不同；本地网关分别记录 `direct` 与 `vpn`；单实例命名互斥体完成同进程和跨进程验证；PyInstaller 免安装程序与 Inno Setup 安装包编译成功。

## Android 包校验

- 包名：`com.routeweaver.app`；版本：`1.2.0`（versionCode 3）。
- minSdk 26；targetSdk / compileSdk 35。
- APK Signature Scheme v2：通过；签名者为 Android Debug（侧载测试签名）。
- 启动 Activity：`com.routeweaver.app.MainActivity`。
- 提供商增强：可枚举声明 `VpnService` 的应用、检测 `com.routeweaver.APPLY_SPLIT_RULES` receiver、显式交接或回退复制清单。
- 规则验证：每条应用/网站规则可按策略读取 VPN 默认出口或绑定物理出口；界面明确标记这是“目标出口基线”，只有提供商确认接收后才代表规则已应用。
- 本机构建环境没有连接 Android 手机或模拟器（`adb devices` 为空），因此完成了 Java 单元测试、资源/DEX 打包、签名和清单校验，但没有声称已做手机实机安装。

## 最终环境状态

- Windows 系统代理：保持测试前值，公开报告已脱敏。
- 路由织网代理所有权标记：不存在。
- 第三方 VPN 客户端仍运行，原本地代理入口仍监听。
